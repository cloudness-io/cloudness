# cloudness

Cloudness platform

### Auto-Generate Cloudness Platform API Client used by UI using Swagger

Please make sure to update the autogenerated client code used by the UI when adding new rest APIs.

To regenerate the code, please execute the following steps:

- Regenerate swagger with latest cloudness platform binary `./cloudness swagger > web/src/services/backend/swagger.yaml`
- navigate to the `web` folder and run `yarn services`

The latest API changes should now be reflected in `web/src/services/code/index.tsx`

UI

<https://windstatic.com/sidebars#>_

Community Components =https://tailwindflex.com/ 


docker run --name cloudness-pg -e POSTGRES_PASSWORD=postgrespwd -e POSTGRES_USER=cloudnessusr -p 5432:5432 -d cloudness



docker build . -t cloudness/cloudness:5

docker run --name cloudness -p 8000:8000 -v /tmp/cloudness:/data -v /tmp/cloudness/kube/config:/.kube -e CLOUDNESS_TRACE=true  cloudness/cloudness:5